services:
  amlwire:
    build: .
    container_name: amlwire
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - /var/log/amlwire.log:/var/log/amlwire.log
    mem_limit: 512m
    cpus: 0.5
